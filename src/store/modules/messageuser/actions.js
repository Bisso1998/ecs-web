import DataAccessor from '@/utils/DataAccessor'
import $ from 'jquery'

export default {

    addMessageTargetUserDetails({ commit, state }, {}) {

    },

    resetMessageTargetUserDetails({ commit, state }, {}) {

    }

}
